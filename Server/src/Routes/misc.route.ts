import { Router } from "express";

const miscRouter = Router()

miscRouter.post("/comment/",()=>{});
miscRouter.delete("/comment/",()=>{});
miscRouter.post("/like",()=>{});

export default miscRouter;